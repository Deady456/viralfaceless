# FreeFaceless

Landing page + setup wizard untuk jasa setup channel YouTube faceless otomatis.

## Hosting

Push ke GitHub Pages:

```bash
git init
git add .
git commit -m "init"
git remote add origin https://github.com/Deady456/freefaceless.git
git push -u origin main
```

Trus Setting → Pages → branch `main`, folder `/`.
